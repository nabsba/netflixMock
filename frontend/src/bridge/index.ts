import {
	serverGetApi,
	serverPost,
	serverGet,
	resultTemplate,
} from './common/requestServer';
import URL_ADDRESSES from './url';

export { URL_ADDRESSES, serverGetApi, serverPost, serverGet, resultTemplate };
