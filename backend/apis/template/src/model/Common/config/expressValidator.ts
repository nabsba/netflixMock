/* eslint-disable @typescript-eslint/no-var-requires */
// To check entry form and avoid hacking
import { check, validationResult } from 'express-validator';

export default {
  check,
  validationResult,
};
